# FileWatcherMonitoring — C# engine port (D365 plugin core)

C# port of the frozen TypeScript Watcher Engine (`apps/watcher/src/engine/`), per
[`docs/superpowers/plans/2026-07-17-ts-to-d365-code-migration.md`](../docs/superpowers/plans/2026-07-17-ts-to-d365-code-migration.md).

## Layout

- `FileWatcherMonitoring.Plugins/` — the engine core: contracts, transition policy
  (allow-list), rule pipeline (duplicate → stuck → stability), `WatcherEngine`,
  `MissingSlaSweep`. **netstandard2.0, zero dependencies** — drops straight into a
  Dataverse plugin assembly.
- `FileWatcherMonitoring.Plugins.Tests/` — xunit suite, **data-driven from
  `docs/superpowers/specs/parity/engine-test-vectors.json`**, which is generated by
  executing the TS reference engine (`npm run parity:vectors -w @apps/watcher`).
  Plus property tests for the random-ID cases.

## Status / caveat

The vectors are generated and verified against the reference engine in this repo's CI-able
toolchain. The C# projects however have **not been compiled here** — this sandbox has no
.NET SDK (network policy blocks the Microsoft download hosts). First `dotnet test` run
happens in the client dev environment:

```bash
dotnet test d365/FileWatcherMonitoring.Plugins.Tests
```

When it's green, tick the matching rows in
[`docs/superpowers/specs/parity/engine-test-parity.md`](../docs/superpowers/specs/parity/engine-test-parity.md).

## What's deliberately NOT here (built in the client environment)

- The `IPlugin` wrapper on `fwm_fileobservation` Create (hydrates `InterfaceConfig` from the
  lookup, calls `WatcherEngine.ProcessObservation`, writes state + event in-transaction).
- `DataverseStateRepository : IStateRepository` (alternate-key upsert via `IOrganizationService`).
- Custom API registrations (`fwm_ProcessObservation`, `fwm_CheckMissingSla`), strong-name key,
  Dataverse SDK package references, FakeXrmEasy plugin-level tests.
